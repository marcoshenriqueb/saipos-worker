services:
  worker:
    build: .
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - n8n_default
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  n8n_default:
    external: true